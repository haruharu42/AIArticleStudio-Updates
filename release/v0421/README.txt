AI Article Studio v0.4.2.1

Purpose:
- Fix the article creation screen so the Phase 3.6 image generation settings card is actually visible.
- Also show the existing Web AI settings card when present.

Base version:
- Requires installed v0.4.2.

Safety:
- Canonical live application path only; backup_auto_* is not scanned.
- No article data is deleted.
- No API/local image generation is enabled by this hotfix.
- Existing Web image prompt workflow remains unchanged.
